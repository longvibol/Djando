- To perform : Sorting , Min , Max , Avg 

1- Import Avg

from django.db.models import Avg,Min

- create function to return aggregate :


def index(request):
    books = Book.objects.all().order_by("rating")
    num_books = books.count()
    avg_rating = books.aggregate(Avg("rating"), Min("rating")) #rating_avg, rating_min

    return render(request, "book_outlet/index.html",{
        "books" : books,
        "total_number_of_books": num_books,
        "average_rating": avg_rating
    })


- num_books = books.count()
- avg_rating = books.aggregate(Avg("rating"), Min("rating")) #rating_avg, rating_min

2- in the html 

    <p>Total Number Of Books: {{ total_number_of_books }}</p>
    <p>Average Rating: {{ average_rating.rating__avg}}</P>




